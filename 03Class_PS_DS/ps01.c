#include<stdio.h>

int main(){
    int n,i,j,rows,k=0;
 printf("Enter the number of rows\n");
 scanf("%d",& rows);
 n=(rows+1)/2;
 for(i=1;i<=rows;i++)
 if(rows%2==0){
    if(i<=n)
    k++;
    if(i>n+1)
    k--;
    for(j=1;j<=rows;j++){
   if(j>=n+1-k&&j<=s
    }
 }

    
}